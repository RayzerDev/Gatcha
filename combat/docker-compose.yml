services:
  mongodb:
    extends:
      file: ../docker/api.docker-compose.base.yml
      service: mongodb
    container_name: mongodb-combat-local
    ports:
      - "27021:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
