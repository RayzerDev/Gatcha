services:
  mongodb:
    extends:
      file: ../docker/api.docker-compose.base.yml
      service: mongodb
    container_name: mongodb-invocation-local
    ports:
      - "27020:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
