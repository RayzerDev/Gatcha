services:
  mongodb:
    extends:
      file: ../docker/api.docker-compose.base.yml
      service: mongodb
    container_name: mongodb-auth-local
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
